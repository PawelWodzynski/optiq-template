import React from 'react';
import styles from './LoginHeader.module.css';

const LoginHeader = () => {
  return (
    <h1 className={styles.loginHeader}>Auth React JWT</h1>
  );
};

export default LoginHeader;

